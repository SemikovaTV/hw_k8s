apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: vol2
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
